# Control Loop Notes

The fictional control loop receives inspection readings from a packaging station and returns one of
three routing decisions:

- `accept` when confidence is high and no defect is detected.
- `review` when confidence is low.
- `reject` when a defect is detected.

These notes exist only to exercise Vaultwright's repo mirror readability.
