# Maintenance log

Append-only; newest at the bottom. One greppable line per change:
`grep "^## \[" log.md | tail`. Format `## [YYYY-MM-DD] <op> | <what>`
(op = `ingest` · `query` · `lint` · `sync` · `restructure` · `init`).

## [2026-01-01] init | vault created from the Vaultwright template
